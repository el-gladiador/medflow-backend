# MedFlow Environment Configuration Example
# Copy this file to .env and customize for your environment
#
# Environment files:
#   .env.local             - Local development (outside Docker)
#   .env.docker            - Docker Compose development
#   .env.staging.example   - AWS staging template
#   .env.production.example - AWS production template

# Environment (development | staging | production)
MEDFLOW_SERVER_ENVIRONMENT=development

# Database Credentials (used by Docker Compose and migrations)
DB_USER=medflow
DB_PASSWORD=devpassword
DB_HOST=localhost
DB_SSL_MODE=disable

# RabbitMQ password (used by Docker Compose)
RABBITMQ_PASSWORD=devpassword

# JWT secret for token signing
# IMPORTANT: Use a strong, random secret in production!
# Generate with: openssl rand -base64 64
JWT_SECRET=dev-secret-change-in-production-use-strong-random-string

# 12-Factor Style Database URLs (alternative to individual fields)
# Set these to override the default localhost connections
# AUTH_DATABASE_URL=postgres://medflow:devpassword@localhost:5433/medflow_auth?sslmode=disable
# USER_DATABASE_URL=postgres://medflow:devpassword@localhost:5434/medflow_users?sslmode=disable
# STAFF_DATABASE_URL=postgres://medflow:devpassword@localhost:5435/medflow_staff?sslmode=disable
# INVENTORY_DATABASE_URL=postgres://medflow:devpassword@localhost:5436/medflow_inventory?sslmode=disable
